version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./sqlite.db:/app/sqlite.db
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
